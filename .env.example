# Copie para .env (o docker-compose lê automaticamente). Não comite o .env real.
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=venues
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123

# Portas de host (opcional) — descomente/ajuste se as padrão estiverem ocupadas
# FRONTEND_PORT=3000
# BACKEND_PORT=8080
# POSTGRES_PORT=5432
# MINIO_API_PORT=9000
# MINIO_CONSOLE_PORT=9001
# RABBITMQ_PORT=5672
# RABBITMQ_MGMT_PORT=15672
